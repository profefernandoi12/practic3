<form method="post" action="{{ path('app_cursada_delete', {'id': cursada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cursada.id) }}">
    <button class="btn">Borrar</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Provincia{% endblock %}

{% block body %}
    <h1>Provincia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ provincium.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ provincium.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_provincia_index') }}">back to list</a>

    <a href="{{ path('app_provincia_edit', {'id': provincium.id}) }}">edit</a>

    {{ include('provincia/_delete_form.html.twig') }}
{% endblock %}

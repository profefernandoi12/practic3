<form method="post" action="{{ path('app_instituto_delete', {'id': instituto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instituto.id) }}">
    <button class="btn">Delete</button>
</form>

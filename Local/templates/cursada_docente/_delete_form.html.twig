<form method="post" action="{{ path('app_cursada_docente_delete', {'id': cursada_docente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cursada_docente.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_asignatura_delete', {'id': asignatura.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asignatura.id) }}">
    <button class="btn">Delete</button>
</form>

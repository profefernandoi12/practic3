<form method="post" action="{{ path('app_preinscripcion_delete', {'id': preinscripcion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ preinscripcion.id) }}">
    <button class="btn">Eliminar</button>
</form>

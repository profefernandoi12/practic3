{% extends 'base.html.twig' %}

{% block title %}OfertaEducativa{% endblock %}

{% block body %}
    <h1>OfertaEducativa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ciclo_lectivo</th>
                <td>{{ oferta_educativa.cicloLectivo }}</td>
            </tr>
            <tr>
                <th>Cupo</th>
                <td>{{ oferta_educativa.cupo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_oferta_educativa_index') }}">Volver</a>

    <a href="{{ path('app_oferta_educativa_edit', {'id': oferta_educativa.id}) }}">Editar</a>

    {{ include('oferta_educativa/_delete_form.html.twig') }}
{% endblock %}

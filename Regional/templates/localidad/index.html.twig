{% extends 'base.html.twig' %}

{% block title %}Localidades{% endblock %}

{% block body %}
    <h1>Localidades</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Codigo_postal</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for localidad in localidads %}
            <tr>
                <td>{{ localidad.nombre }}</td>
                <td>{{ localidad.codigoPostal }}</td>
                <td>
                    <a href="{{ path('app_localidad_show', {'id': localidad.id}) }}">Mostrar</a>
                    <a href="{{ path('app_localidad_edit', {'id': localidad.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no se encontraron datos</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_localidad_new') }}">Crear</a>
{% endblock %}
